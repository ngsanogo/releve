# mypy for custom_components/releve, in the Home Assistant environment. Separate
# from pyproject.toml's [tool.mypy], which targets releve's Python and plugins.
[mypy]
python_version = 3.14
strict = True
warn_unreachable = True
